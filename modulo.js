import {nombre} from './funciones.js'
import {apellido} from './callback.js'

// aca hacemos un callback que conciste en enviar por parametro una funcion a otra funcion, para que estga otra funcion pueda usar lirbemente la funcion que va por parametro
nombre("fernando",  apellido);